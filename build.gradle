apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'maven'
apply plugin: 'war'
apply plugin: 'eclipse-wtp'

repositories {
	mavenCentral()
}

dependencies {
	compile 'com.google.guava:guava:12.0'
	compile 'org.apache.tomcat:tomcat-servlet-api:7.0.12'
}

test.dependsOn(war)

war {
	archiveName = "zxf.asyncservice#v1.war";
}

war.doLast {
	println "Exploding WAR!!!! BOOM!"
	ant.unzip(src: war.archivePath, dest: "$buildDir/web")
}

eclipse {
	wtp {
		component {
		}
	}
}